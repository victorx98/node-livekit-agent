services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped

  agent:
    build: .
    env_file: .env.local
    environment:
      REDIS_URL: redis://redis:6379
    ports:
      - "8080:8080"
      - "8081:8081"
    depends_on:
      - redis
    restart: unless-stopped
